## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth animations for page transitions and components
clsx | Utility for conditional classes (standard in shadcn but good to be explicit)
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication uses Replit Auth (passport).
API endpoints are defined in @shared/routes.
Image uploads are handled via Base64 strings sent to /api/reports.
Theme should be professional "Police/Security" (blues, grays, clean).
